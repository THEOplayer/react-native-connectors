// Web entry point of the app.
//
// This file is intentionally a thin, JSX-free wrapper: cavynext swaps
// `index.web.js` and `index.test.web.js` to boot the app into its test runner,
// and it only accepts entry files ending in `.js`.
import './web/app.web';
